node_modules
*.log
.DS_Store
tasks/
gallery-dist/

# parallel-workflow scratch: /feat worktrees + dashboard capture worktree
.worktrees/
.captures/

# design-sync — staged scripts, build output, machine state. Durable inputs under
# .design-sync/ NOT listed here stay committed (config.json, NOTES.md,
# conventions.md, previews/, overrides/).
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
.design-sync/compiled.css
