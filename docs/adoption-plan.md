# Adoption Plan

Generated by `/adopt-workflow` on 2026-07-05. design-baseline was a `git init`'d but
unversioned-by-convention donor; this migration brings it into the standard doc convention.
**Purely additive** — no `git mv`, no splits, no deletions.

## Summary

- 0 files moved · 0 rewritten · 8 created · 0 deleted
- The repo already had `docs/backlog/` (+ `archive/`), `docs/archetypes/`, `docs/superpowers/{specs,plans}/`,
  and a rich set of design docs (STYLE, TAXONOMY, PLUGIN-CONTRACT, FLEET-AUDIT, …). It lacked only the
  convention's entry points: `CLAUDE.md`, `ARCHITECTURE.md`, `RULES.md`, `lessons.md`, `adr/`, `backlog/wip/`,
  and the archive-warn hook.

## CLAUDE.md
**Found:** none (README.md only). **Action:** create.
**Plan:** Declares `## Doc Paths` (todo/lessons/backlog/architecture/decisions/rules/specs/plans +
design-baseline-specific `archetypes:`/`audits:`), the donor role, local-only lifecycle, and the
contract-vs-reference split as a project note.

## Architecture
**Found:** none at the convention path. **Action:** create `docs/ARCHITECTURE.md`.
**Plan:** Real initial map (stack, component map, 14 MANIFEST archetypes, contract/reference split,
promotion flow, fleet-audit system, open questions). Not a stub.

## Decisions
**Found:** none (no `DECISIONS.md`, no `docs/adr/`). **Action:** create empty `docs/adr/INDEX.md`.
**Plan:** No legacy to split. ADRs accrue going forward (the promotion maturity gate already references
"a governing ADR").

## Rules
**Found:** hard rules scattered across `docs/archetypes/README.md`, the promotion spec, and
`promote-archetype.md`. **Action:** create `docs/RULES.md`.
**Plan:** 11 sourced hard rules (baseline-never-originates, two-doc split, contract purity, maturity
gates, version-field semantics, /feat-worktree writes, …) + a Guidelines section.

## Backlog
**Found:** 2 root tickets + 20 archived; **no `wip/`**. **Action:** create `docs/backlog/wip/`.
**Plan:** Add `wip/` (`.gitkeep`) so the state machine has all three lanes. Existing tickets untouched.
Post-migration, new tickets go through `/ticket`, not hand-written files.

## Tasks
**Found:** `tasks/` already gitignored, no tracked files. **Action:** leave.

## Hooks
**Found:** none. **Action:** install `.claude/hooks/warn-backlog-archive.sh` (from template).

## Cleanup (proposed deletions)
- None.

## Risk notes / follow-ups (not blocking this migration)
- **`report` + `calendar` MANIFEST entries** carry `authored`, violating the "baseline never originates
  archetypes" rule — pre-existing; flag for a reconciliation ticket.
- **`promote-archetype` writes to the donor's primary checkout on `main`**, which the parallel-safe hook
  now rejects — the skill needs rewiring to write in a `/feat` worktree (tracked as dashboard Phase 3.6).
- **No `docs/backlog/README.md`** documenting this repo's ticket frontmatter schema.
- `.design-sync/` relationship to the gallery/hub path is not fully understood (see ARCHITECTURE §9).

_Transitional artifact — delete `docs/adoption-plan.md` in a follow-up commit once you're satisfied._
